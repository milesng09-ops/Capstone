# Requires Python 3.10 or newer.
#
# pip cannot enforce this from a requirements file, so the check lives in
# app/__init__.py and fails with an actionable message. The floor is PEP 604
# union syntax (`str | None`), which Pydantic and SQLAlchemy evaluate at model
# build time. Note that the system python3 on macOS is often 3.9.

fastapi>=0.115.0
uvicorn[standard]>=0.30.6
pydantic>=2.9.0
pydantic-settings>=2.5.2
SQLAlchemy>=2.0.35
pandas>=2.2.0
numpy>=1.26.0
httpx>=0.27.2
python-dateutil>=2.9.0
tzdata>=2024.1

# Optional provider dependency. The application degrades to demo data if it is
# missing, so installation failures here are not fatal.
yfinance>=0.2.44

# Testing
pytest>=8.3.3
pytest-asyncio>=0.24.0
